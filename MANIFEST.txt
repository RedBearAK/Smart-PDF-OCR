MANIFEST.txt -- hourglass tarball
Apply into: repo root (dev_beta)
Apply after: timing

FILES (full drop-in replacement)

  src/smart_pdf_ocr/cli.py

WHAT CHANGES

  The pool's progress line now reads "pages recognized 61/82" instead of
  "recognizing page 61/82". A pool finishes pages in whatever order they
  complete, so the counter is an hourglass filling, not a cursor on a page;
  the old phrasing implied a position that no longer exists. The sequential
  path keeps "recognizing page X/Y" -- there it genuinely is on that page --
  and rasterization keeps "rasterizing page X/Y" for the same reason. The
  _progress helper takes a full label now instead of a verb, so word order
  is free.

  Nothing else changes; no test updates needed (no test pins the phrasing).
  Suite after application: 122 passed, pyflakes clean.

# End of file #
